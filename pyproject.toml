[project]
name = "stripe"
version = "0.1.0"
description = "Official Stripe MCP integration for Prime Agent (mcp.stripe.com)."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stripe"]
